# Security (Ignored forever)
.env
_OLD_MESS/

# Internal planning docs — must NOT ship to the (fully static-served) app root
docs/
.claude/

# Home-worker setup prose (infra disclosure) — keep local, hand to the home PC
# directly; the worker .py stays tracked so the home PC can clone it.
scripts/*.md

# Python build junk
__pycache__/
*.pyc

# System Junk
.DS_Store
*.log

# Dependencies (Too heavy for Git)
node_modules/

# Runtime data (lives on the server, grows over time)
translation-cache.json
tts-cache/
